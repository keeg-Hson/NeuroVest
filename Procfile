# Railway Procfile - Tells Railway how to run services
# NOTE: Railway will use this for auto-detection

web: streamlit run api_demo.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true
