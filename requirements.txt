# Core ML/Data (NumPy 2.x compatible)
numpy>=1.26.4  # Supports both 1.x and 2.x
pandas>=2.2.2  # NumPy 2.x compatible
scikit-learn>=1.5.1  # NumPy 2.x compatible
imbalanced-learn>=0.12.3
xgboost>=2.0.3  # NumPy 2.x compatible
lightgbm>=4.1.0  # NumPy 2.x compatible
catboost>=1.2  # Gradient boosting ensemble member
matplotlib>=3.8.4
seaborn>=0.13.0  # For correlation heatmaps
scipy>=1.14.0  # NumPy 2.x compatible

# Feature engineering
ta>=0.10.2

# Data sources
yfinance>=0.2.43
multitasking>=0.0.11  # Required by yfinance
ccxt>=4.2.25
pandas-datareader>=0.10.0  # Alternative data source
PyYAML>=6.0  # For configuration files

# Utilities
requests>=2.32.3
python-dotenv>=1.0.1
APScheduler>=3.10.4
schedule>=1.2.0
joblib>=1.4.2

# API (optional - for REST API server)
fastapi>=0.109.0
uvicorn>=0.27.0
pydantic>=2.5.3
websockets>=12.0

# Data I/O (NumPy 2.x compatible)
duckdb>=1.1.3
pyarrow>=17.0.0  # 17.0+ required for NumPy 2.x compatibility

# External signals / NLP
alpha_vantage>=2.3.1
textblob>=0.17.1
nltk>=3.8.1

# Testing
pytest==8.0.0
pytest-cov==4.1.0

# Dev tools
black==24.8.0
ruff==0.6.9

# Optional - Advanced ML models (not required for core functionality)
# hmmlearn>=0.3.0  # For regime switching models (train_regime_switching_model.py)
# focal-loss>=0.0.7  # For focal loss training (train_lstm_focal_loss.py)
# tensorflow>=2.13.0  # For LSTM models (optional)
